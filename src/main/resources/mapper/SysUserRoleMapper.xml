<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ms.hms.mapper.SysUserRoleMapper">
    <select id="getRoleUsedCount" resultType="java.util.Map">
        SELECT r.NAME as roleName, COUNT( urr.id ) AS count
        FROM tb_sys_role r INNER JOIN tb_sys_user_role_relation urr ON r.id = urr.role_id
        GROUP BY roleName;
    </select>
</mapper>
